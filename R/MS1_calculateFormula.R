

calculateFormula <- function(mz, adducts, isotopePattern = NULL, mzTol = 0.005, mzTolType = "abs", formulaSettings) {


  # List with settings for formulas, e.g. C/H and C/O ratio
  # TODO

  # check which branch
  if(is.null(isotopePattern)) {

    # decompose mass

  } else {

    # decompose pattern

  }


}
